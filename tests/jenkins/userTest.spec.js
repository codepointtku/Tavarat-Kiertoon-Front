// import { test, expect } from '@playwright/test';

/* test('create new user', async ({ page }) => {
        await page.goto('/');
        await page.locator('#appbar-containing-div').getByRole('button').nth(1).click();
        await page.getByRole('link', { name: 'Luo uusi tunnus' }).click();
        await page
            .getByRole('link', { name: 'Käyttäjätili on tarkoitettu henkilökohtaiseksi hankinta-tiliksi.' })
            .click();
        await page.getByPlaceholder('sinä@turku.fi').click();
        await page.getByPlaceholder('sinä@turku.fi').fill('tonipal@turku.fi');
        await page.getByPlaceholder('Tonipal').click();
        await page.getByPlaceholder('Tonipal').fill('ToniPal');
        await page.getByPlaceholder('Kahville').click();
        await page.getByPlaceholder('Kahville').fill('Kahville');
        await page.getByPlaceholder('010 111 1111').click();
        await page.getByPlaceholder('010 111 1111').fill('010101010101');
        await page.getByPlaceholder('Kahvikuja 5 as. 666').click();
        await page.getByPlaceholder('Kahvikuja 5 as. 666').fill('Kahvikuja 5 as. 666');
        await page.getByPlaceholder('20100').click();
        await page.getByPlaceholder('20100').fill('20100');
        await page.getByLabel('Kaupunki *').click();
        await page.getByLabel('Kaupunki *').fill('Turku');
        await page.getByLabel('Salasana *').click();
        await page.getByLabel('Salasana *').fill('kahvi');
        await page.getByLabel('Salasana uudelleen *').click();
        await page.getByLabel('Salasana uudelleen *').fill('kahvi');
        await page.getByRole('button', { name: 'Rekisteröidy' }).click();
        await expect(page.getByText('Tunnuksen luominen onnistui')).toBeVisible();

    }); */
