import { Typography } from '@mui/material';

function GuideCommon() {
    return (
        <>
            <Typography>tavis</Typography>
            <Typography>moro</Typography>
            <Typography>moro</Typography>
        </>
    );
}

export default GuideCommon;
