import { Typography } from '@mui/material';

function GuideAccount() {
    return (
        <>
            <Typography>käyttäjät</Typography>
            <Typography>moro</Typography>
            <Typography>moro</Typography>
        </>
    );
}

export default GuideAccount;
