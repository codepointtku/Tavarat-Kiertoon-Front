import { Container, Typography } from '@mui/material';
// import {CustomizedTime}

function OrderPage() {
    return (
        <Container>
            <Typography variant="overline">OrderPage</Typography>
        </Container>
    );
}

export default OrderPage;
