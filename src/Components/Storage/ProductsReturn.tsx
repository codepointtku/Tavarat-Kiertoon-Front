interface Props {
    id: number;
}

function ProductsReturn({ id }: Props) {
    // submit id and returning amount
    // show picture for visual confirmation
    // show
    return <div>ProductsReturn: {id}</div>;
}

export default ProductsReturn;
