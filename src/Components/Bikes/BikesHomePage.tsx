import { Typography } from '@mui/material';

export default function BikesHomePage() {
    return (
        <Typography variant="h4" component="h1" sx={{ fontFamily: 'Montserrat', color: 'primary.main' }}>
            teretulemas bikes pages
        </Typography>
    );
}
