import { Box } from '@mui/material';

function ComponentContainer() {
    return (
        <Box id="component-render-area" sx={{ border: '1px solid red' }}>
            kompsut
        </Box>
    );
}

export default ComponentContainer;
