import { Box, Typography } from '@mui/material';

function SummaryUsers() {
    return (
        <Box id="summary-component-users" sx={{ border: '1px solid cyan', padding: '1rem' }}>
            <Typography color="#000">summary usersseista</Typography>
        </Box>
    );
}

export default SummaryUsers;
