import { Box } from '@mui/material';

function SummaryProducts() {
    return (
        <Box id="summary-component-products" sx={{ border: '1px solid cyan', padding: '1rem' }}>
            summary productseista
        </Box>
    );
}

export default SummaryProducts;
