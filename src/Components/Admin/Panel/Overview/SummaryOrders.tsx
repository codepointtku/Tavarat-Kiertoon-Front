import { Box } from '@mui/material';

function SummaryOrders() {
    return (
        <Box id="summary-component-orders" sx={{ border: '1px solid cyan', padding: '1rem' }}>
            summary ordersseista
        </Box>
    );
}

export default SummaryOrders;
