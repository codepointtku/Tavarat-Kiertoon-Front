import { Box } from '@mui/material';

function NavigationTree() {
    return (
        <Box id="navigation-tree" sx={{ border: '1px solid green' }}>
            nav tree
        </Box>
    );
}

export default NavigationTree;
